package proxy

import "testing"

// We have an API service, so we test end to end with the Makefile.
// There are no individual components to test, yet.
func TestStub(t *testing.T) {
	t.Log("TODO")
}
